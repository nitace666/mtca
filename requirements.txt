# MTCA 依赖（最小集）

# 核心存储
# SQLite + FTS5 是 Python 内置，零依赖

# CLI 可视化
rich>=13.0
click>=8.0

# 测试
pytest>=7.0
pytest-cov>=4.0
pytest-benchmark>=4.0

# M2 起才会用到，本文件保留占位
# mcp>=1.0
# httpx>=0.25
# fastapi>=0.100
