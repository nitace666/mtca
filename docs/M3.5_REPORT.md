# MTCA M3.5 交付报告（2026-07-08）

## 概述

本交付覆盖 M3-5（向量化召回层）+ openclaw MCP HTTP transport + 3 个 bug 修复。
起自 faad3d2，结束于 dcb504d，6 commit / 9 文件 / 2154 行 / 633 测试全过 / 0 回归。

## 交付清单

| # | commit | 内容 | 行数 |
|---|---|---|---|
| 1 | adf0967 | M3-5 P0（向量召回 + ChromaDB + Ollama nomic-embed）| 1195 |
| 2 | 77f5a06 | recall_engine 向量块位置 + cache key 含 vector 状态（B3 + B4）| 280 |
| 3 | df0ecad | FTS5 unicode61 转 trigram 中文 token 化（v4 migration）| 220 |
| 4 | be5ce53 | 整合 1-3 到 main | merge |
| 5 | 2666382 | openclaw HTTP transport（MCP 2025-03-26，0 新依赖）| 460 |
| 6 | dcb504d | 整合 5 到 main | merge |

## 实战能力

- MTCA 向量化召回层：p95 = 4.2ms，配置 vector_recall_enabled=1 即可启用
- FTS5 中文召回提升：trigram query 大于等于 3 字符命中（之前 unicode61 整段当 1 token，命中率约 0）
- openclaw 客户端可调 MTCA 6 tool：recall_segments / search_facts / mark_important / fog_segment / get_recent_sessions / list_llm_backends
- 端到端 openclaw 真连测：initialize HTTP 200 / tools/list 返 6 个 tool / tools/call list_llm_backends 真返 LLM 后端探测结果

## 测试基线

main 633 PASS in 118.74s（606 baseline + 13 vector + 2 B3B4 + 4 B2 + 8 openclaw = 633）

## 已知 spec bug 留档（仍待修 / 后续任务）

- FTS5 trigram 对 2 字符 query 不命中（probe 实测，trigram 需要 query 大于等于 3 字符；2 字中文 query 仍 0 命中，保留缺口）
- vector_cache schema 验证缺失（Step 3 仅写 v3 migration + 5 行 schema 描述，未加表结构 unit test；后续 P1 加）
- MTCA MCP Server 工具默认 127.0.0.1:8765（openclaw 配置默认 URL；端口冲突场景已用 _find_free_port() 规避，记档）

## Worktree 状态

合并后已清理 4 个 worktree：

- I:\PROJECTS\MTCA-m3-5-vector（adf0967 已并入 main，清理）
- I:\PROJECTS\MTCA-bugfix-vector-recall-engine（77f5a06 已并入 main，清理）
- I:\PROJECTS\MTCA-bugfix-fts5-tokenize（df0ecad 已并入 main，清理）
- I:\PROJECTS\MTCA-mcp-openclaw（2666382 已并入 main，清理）

## 后续里程碑建议

1. M5 开源发布（README + LICENSE + 文档整理）
2. M3-1 sync 接口 stub（DEFER 中）
3. 接更多 MCP 客户端（Hermes / Trae 等；HTTP transport 通用）
4. 真数据 benchmark（等老板 L0 真历史对话，重跑 real benchmark）
5. vector_cache schema test 补全（P1 任务）
6. FTS5 trigram 2 字 query 增强（可能要 jieba 分词 + custom tokenize，P1 评估）

## 关键文件清单

```
src/recall/vector_index.py              (Step 3, +302 行)
src/store/sqlite.py                     (Step 3+5+8, +118 行 v3+v4 migration + import)
src/recall/recall_engine.py             (Step 4+7, +213 行 向量块 + _fuse_3way_rrf + B3+B4 修复)
src/adapters/openclaw_http_adapter.py   (Step 10, +296 行 openclaw HTTP transport)
src/adapters/mcp_server.py              (既有, T31 6 tool 实现)
tests/test_vector_recall.py             (Step 3, 13 测试)
tests/test_recall_vector_fallback.py    (Step 7, 2 测试 B3+B4)
tests/test_fts5_trigram.py              (Step 8, 4 测试 B2)
tests/test_openclaw_http_integration.py (Step 10, 8 测试 openclaw)
benchmarks/_m35_vector_benchmark.py     (Step 5, 284 行 Jaccard + p95 benchmark)
docs/VECTOR_RECALL.md                    (Step 6, 150 行 使用说明)
docs/M3.5_REPORT.md                      (本文件)
requirements.txt                         (Step 6, +1 行 chromadb>=0.5)
```
