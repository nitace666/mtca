# MTCA M1 P0 修复 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 M1 验收报告识别的 3 个 P0 致命问题，让 `pip install -e .` 真正跑通。

**Architecture:** 按"安全 → 入口 → 清理"顺序修复。A1 移除硬编码凭证（环境变量化）；A2 把 `mtca-server.py` 移到 `src/cli/server.py` 模块化（pyproject.toml 不动）；A3 删除 0 字节的 `setup.py`。每阶段独立可测，最后跑 `pip install -e .` + `mtca-server --help` 闭环。

**Tech Stack:** Python 3.10+ / Click / setuptools / pytest / 已有 dev 依赖

## Global Constraints

- AI_RULES.md §6.3 禁止引入新依赖——只用现有 `click`
- 所有 SQL 走 `?` 占位符（这次不涉及 SQL，列出防新代码违规）
- 中文错误信息 / 中文 docstring
- 单文件 ≤ AI_RULES.md §5.6 规定行数（默认 1000）
- 锁定文件不可改：`README.md` / `LICENSE` / `.gitignore` / `AI_RULES.md` / `PLAYBOOK.md` / `DEVELOPER_PLAN.md` / `docs/*.md`（plan 文件放 `docs/superpowers/plans/` 不算）
- 每次 commit 一个 T 任务 / 一个 Stage；message 中文 `T{stage}: 简述`
- pytest 全过才能 commit
- 失败 → 报告 + 等用户指令，AI 不要自动改

## 文件结构

| 路径 | 角色 | 操作 |
|---|---|---|
| `mtca-server.py` | 根目录旧入口（48 行 stub）| A2 移到模块后删除 |
| `src/cli/server.py` | 新模块化入口（替代 mtca-server.py）| A2 创建 |
| `pyproject.toml` | 已改 entry point 为 cli.server:main（T_A2_fix：src/ 无 __init__.py，setuptools.packages.find 自动找 cli 包） |
| `setup.py` | 0 字节空文件 | A3 删除 |
| `tests/test_mtca_server.py` | 新建：A1 凭证加载测试 | A1 创建 |
| `tests/__init__.py` | 现有 | 不动 |

---

## Stage 0: 基线确认（必做，先执行）

**Files:** 不动任何文件，只检查

**Goal:** 拍下修复前快照，确保 Stage 1-3 真的修了问题

- [ ] **Step 1: 跑现有测试看基线**

```bash
cd I:\PROJECTS\MTCA
python -m pytest tests/ -q --tb=no
```

**预期**：296 passed, 0 failed（如不一致先排查再继续）

- [ ] **Step 2: 拍 A1 凭证泄漏证据**

```bash
cd I:\PROJECTS\MTCA
Select-String -Path mtca-server.py -Pattern 'api_key|proxy|project_id|app_name'
```

**预期**：4 行命中（api_key / proxy / app_name / project_id 各 1 行）

- [ ] **Step 3: 拍 A2 入口断裂证据**

```bash
cd I:\PROJECTS\MTCA
Test-Path src\cli\server.py
```

**预期**：False（不存在）

- [ ] **Step 4: 拍 A3 空文件证据**

```bash
cd I:\PROJECTS\MTCA
(Get-Item setup.py).Length
```

**预期**：0

**完成判定**：4 个 Step 输出与预期完全一致 → 进 Stage A1

---

## Stage A1: 修复 mtca-server.py 硬编码凭证（安全 P0）

**Files:**
- Modify: `mtca-server.py:9-14`（删除 4 行硬编码 + 改为条件加载）
- Create: `tests/test_mtca_server.py`（验证无 env 变量时跳过 initialize）

**Interfaces:**
- Consumes: `os.environ`（环境变量读取）
- Produces: `mtca-server.py` 在 env 变量缺失时**不调用** `syncause_tracer.initialize()`

- [ ] **Step 1: 写失败测试（TDD 红）**

文件：`tests/test_mtca_server.py`

```python
"""tests/test_mtca_server.py — Stage A1 红绿测试。

验证 mtca-server.py 在没有 MTCA_TRACE=1 时不会调 syncause_tracer.initialize()。
"""
import os
import importlib
import sys
from unittest import mock

import pytest


def test_no_env_means_no_initialize_call(monkeypatch):
    """未设 MTCA_TRACE=1 时，绝不调用 syncause_tracer.initialize。"""
    # 1. 清掉所有相关 env
    for k in ("MTCA_TRACE", "SYNCAUSE_API_KEY", "SYNCAUSE_PROXY", "SYNCAUSE_PROJECT_ID"):
        monkeypatch.delenv(k, raising=False)
    
    # 2. mock syncause_tracer 让它被调就报错
    fake_syncause = mock.MagicMock()
    fake_syncause.initialize = mock.MagicMock(side_effect=AssertionError(
        "BUG: initialize() called without MTCA_TRACE=1"
    ))
    
    with mock.patch.dict(sys.modules, {"syncause_tracer": fake_syncause}):
        # 3. 重新 import mtca_server 模块（如果是模块；脚本则直接 exec）
        if "mtca_server" in sys.modules:
            del sys.modules["mtca_server"]
        # 脚本是 mtca-server.py（下划线变下划线），用 importlib 直接加载
        spec = importlib.util.spec_from_file_location(
            "mtca_server", "mtca-server.py"
        )
        mod = importlib.util.module_from_spec(spec)
        spec.loader.exec_module(mod)
    
    # 4. 断言没被调
    fake_syncause.initialize.assert_not_called()


def test_with_env_calls_initialize(monkeypatch):
    """设 MTCA_TRACE=1 + 3 个凭证 → 应调 initialize。"""
    monkeypatch.setenv("MTCA_TRACE", "1")
    monkeypatch.setenv("SYNCAUSE_API_KEY", "fake-key")
    monkeypatch.setenv("SYNCAUSE_PROXY", "wss://test/ws")
    monkeypatch.setenv("SYNCAUSE_PROJECT_ID", "test-uuid")
    
    fake_syncause = mock.MagicMock()
    with mock.patch.dict(sys.modules, {"syncause_tracer": fake_syncause}):
        if "mtca_server" in sys.modules:
            del sys.modules["mtca_server"]
        spec = importlib.util.spec_from_file_location(
            "mtca_server", "mtca-server.py"
        )
        mod = importlib.util.module_from_spec(spec)
        spec.loader.exec_module(mod)
    
    fake_syncause.initialize.assert_called_once()
    kwargs = fake_syncause.initialize.call_args.kwargs
    assert kwargs["api_key"] == "fake-key"
    assert kwargs["proxy"] == "wss://test/ws"
    assert kwargs["project_id"] == "test-uuid"
    assert kwargs["app_name"] == "MTCA"
```

- [ ] **Step 2: 跑测试确认红**

```bash
cd I:\PROJECTS\MTCA
python -m pytest tests/test_mtca_server.py -v
```

**预期**：2 个测试 FAIL（红）— 现有代码会无条件调 initialize

- [ ] **Step 3: 改 mtca-server.py 第 9-14 行**

将：
```python
from syncause_tracer import initialize
initialize(
    api_key="sysk_nIgjXwFbIDmYkLTcUc36nFrZJ7Gpq67",
    proxy="wss://api.syn-cause.com/codeproxy/ws",
    app_name="MTCA",
    project_id="56252d81-82ff-49be-bcc4-2e8e0c1900c4"
)
```

替换为：
```python
import os

# 可选埋点：仅当 MTCA_TRACE=1 + 3 个凭证齐全时才启用
# 默认不安装 syncause_tracer；用户想启用自行 `pip install syncause-tracer`
if os.environ.get("MTCA_TRACE") == "1":
    from syncause_tracer import initialize
    initialize(
        api_key=os.environ["SYNCAUSE_API_KEY"],
        proxy=os.environ["SYNCAUSE_PROXY"],
        app_name="MTCA",
        project_id=os.environ["SYNCAUSE_PROJECT_ID"],
    )
```

同时把文件最顶部 `#!/usr/bin/env python3` 下面 4 行 docstring 之后，**在 `import` 前加** `import os`（避免 import 顺序错乱）。

最终文件结构：
```python
#!/usr/bin/env python3
"""
MTCA Server 启动入口（M1 阶段占位）

...（原 docstring 保留）...
"""
import os  # ← 新增

# 可选埋点（同上 8 行代码块）

import sys
import click
from src.store.sqlite import init_db, MTCA_DB_PATH
# ... 后面原样
```

- [ ] **Step 4: 跑测试确认绿**

```bash
cd I:\PROJECTS\MTCA
python -m pytest tests/test_mtca_server.py -v
```

**预期**：2 passed

- [ ] **Step 5: 跑全部测试看回归**

```bash
cd I:\PROJECTS\MTCA
python -m pytest tests/ -q --tb=no
```

**预期**：298 passed (296 + 2 新增), 0 failed

- [ ] **Step 6: 拍修复后证据**

```bash
cd I:\PROJECTS\MTCA
Select-String -Path mtca-server.py -Pattern 'api_key|proxy|project_id|app_name'
```

**预期**：无输出（凭证已删除）

- [ ] **Step 7: 提交**

```bash
cd I:\PROJECTS\MTCA
git add mtca-server.py tests/test_mtca_server.py
git commit -m "T_A1: 移除 mtca-server.py 硬编码凭证（改环境变量 + 条件加载）"
```

**完成判定**：Step 4-7 全绿，2 个新测试通过，旧测试 0 回归 → 进 Stage A2

---

## Stage A2: 修复 pyproject.toml 入口断裂（安装 P0）

**Files:**
- Create: `src/cli/server.py`（从 `mtca-server.py` 复制 + 改 entry）
- Delete: `mtca-server.py`（根目录）
- 修改: `pyproject.toml:35` 改为 `cli.server:main`（T_A2_fix）

**Interfaces:**
- Consumes: `src.store.sqlite.init_db / get_stats / MTCA_DB_PATH`
- Produces: `mtca-server` 命令可被 `pip install -e .` 正确安装 + 运行

- [ ] **Step 1: 创建 src/cli/server.py**

新文件 `src/cli/server.py`：

```python
"""MTCA Server 启动入口（src/cli/server.py — T_A2 模块化）

从根目录 mtca-server.py 迁移而来；保持原 CLI 子命令 + 默认行为不变。
pyproject.toml [project.scripts] 已指向 src.cli.server:main。
"""
import os

# 可选埋点：仅当 MTCA_TRACE=1 + 3 个凭证齐全时才启用
# 默认不安装 syncause_tracer；用户想启用自行 `pip install syncause-tracer`
if os.environ.get("MTCA_TRACE") == "1":
    from syncause_tracer import initialize
    initialize(
        api_key=os.environ["SYNCAUSE_API_KEY"],
        proxy=os.environ["SYNCAUSE_PROXY"],
        app_name="MTCA",
        project_id=os.environ["SYNCAUSE_PROJECT_ID"],
    )

import click
from src.store.sqlite import MTCA_DB_PATH, get_stats, init_db


@click.group()
def main():
    """MTCA - Multi-Tier Context Architecture"""
    pass


@main.command()
def init():
    """初始化数据库（创建 schema）"""
    click.echo(f"Initializing MTCA at {MTCA_DB_PATH}")
    init_db()
    click.echo("✓ 数据库已初始化")


@main.command()
def status():
    """查看 MTCA 状态"""
    stats = get_stats()
    click.echo(f"MTCA DB: {MTCA_DB_PATH}")
    click.echo(f"  会话数: {stats.get('sessions', 0)}")
    click.echo(f"  消息数: {stats.get('messages', 0)}")
    click.echo(f"  段落数: {stats.get('segments', 0)}")


@main.command()
def start():
    """启动 MTCA 服务（M1 阶段是 stub）"""
    click.echo("MTCA Server 已启动（M1 stub 模式）")
    click.echo("M2 会接 MCP Server + REST API")
    click.echo("按 Ctrl+C 停止")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 验证模块可独立 import**

```bash
cd I:\PROJECTS\MTCA
python -c "from src.cli.server import main; print('OK:', main)"
```

**预期**：`OK: <Group main>`

- [ ] **Step 3: 验证 CLI 可列出子命令**

```bash
cd I:\PROJECTS\MTCA
python -m src.cli.server --help
```

**预期**：列出 `init / status / start` 三个子命令

- [ ] **Step 4: 删除根目录 mtca-server.py**

```bash
cd I:\PROJECTS\MTCA
Remove-Item mtca-server.py -Force
```

- [ ] **Step 5: 跑全部测试看回归**

```bash
cd I:\PROJECTS\MTCA
python -m pytest tests/ -q --tb=no
```

**预期**：298 passed, 0 failed（无变化，但确保删除没影响其他模块 import）

- [ ] **Step 6: 提交**

```bash
cd I:\PROJECTS\MTCA
git add src/cli/server.py
git add -u mtca-server.py
git commit -m "T_A2: 把 mtca-server.py 移到 src/cli/server.py 模块化（修 pyproject 入口）"
```

**完成判定**：Step 2-3 输出正确，Step 5 全绿，commit 成功 → 进 Stage A3

---

## Stage A3: 删除 0 字节 setup.py（清理 P0）

**Files:**
- Delete: `setup.py`（0 字节空文件）
- No change: `pyproject.toml`（PEP 621 已完整）

- [ ] **Step 1: 确认 setup.py 仍是 0 字节**

```bash
cd I:\PROJECTS\MTCA
(Get-Item setup.py).Length
```

**预期**：0（如不是 0，先排查）

- [ ] **Step 2: 删除 setup.py**

```bash
cd I:\PROJECTS\MTCA
Remove-Item setup.py -Force
```

- [ ] **Step 3: 跑全部测试看回归**

```bash
cd I:\PROJECTS\MTCA
python -m pytest tests/ -q --tb=no
```

**预期**：298 passed, 0 failed

- [ ] **Step 4: 提交**

```bash
cd I:\PROJECTS\MTCA
git add -u setup.py
git commit -m "T_A3: 删除 0 字节 setup.py（pyproject.toml PEP 621 已够）"
```

**完成判定**：Step 3 全绿，commit 成功 → 进 Stage VERIFY

---

## Stage VERIFY: 完整安装 + 启动验收

**Files:** 不动文件

**Goal:** 闭环 M1 验收"安装门槛"指标（pip install -e . + mtca-server 启动）

- [ ] **Step 1: 干净环境重装**

```bash
cd I:\PROJECTS\MTCA
pip uninstall mtca-memory -y 2>&1 | Out-Null
pip install -e .
```

**预期**：`Successfully installed mtca-memory-0.3.0`，**0 error**（如报红立即停下来排查）

- [ ] **Step 2: 验证 mtca-server 命令已注册**

```bash
cd I:\PROJECTS\MTCA
where.exe mtca-server
```

**预期**：输出 `...\Scripts\mtca-server.exe` 路径（如 Python 不是 Windows 脚本适配，跳过此步）

- [ ] **Step 3: 启动 mtca-server --help（看子命令列表）**

```bash
cd I:\PROJECTS\MTCA
mtca-server --help
```

**预期**：`init / status / start` 3 个子命令 + 各自 docstring

- [ ] **Step 4: 初始化 DB**

```bash
cd I:\PROJECTS\MTCA
mtca-server init
```

**预期**：`✓ 数据库已初始化` + DB 路径输出 + 0 error

- [ ] **Step 5: 查看状态**

```bash
cd I:\PROJECTS\MTCA
mtca-server status
```

**预期**：4 行（DB 路径 + 3 个统计），全是 0

- [ ] **Step 6: 跑 pytest 最终回归**

```bash
cd I:\PROJECTS\MTCA
python -m pytest tests/ -q --tb=no
```

**预期**：298 passed, 0 failed

- [ ] **Step 7: 拍所有 3 个 P0 已修复证据**

```bash
cd I:\PROJECTS\MTCA
Write-Host '=== A1 凭证已删 ==='
(Select-String -Path src\cli\server.py -Pattern 'sysk_|syn-cause.com' | Measure-Object).Count

Write-Host '=== A2 入口 OK ==='
python -c "from src.cli.server import main; print(main)"

Write-Host '=== A3 setup.py 已删 ==='
Test-Path setup.py

Write-Host '=== git log 最近 4 个 ==='
git log --oneline -4
```

**预期**：
- A1: 0
- A2: `<Group main>`
- A3: False
- git log: 看到 T_A1 / T_A2 / T_A3 三个新 commit

**完成判定**：7 个 Step 全绿 → M1 P0 全部修复完毕

---

## 风险与兜底

| 风险 | 兜底 |
|---|---|
| Stage A1 测试 red 时旧代码无 syncause_tracer | 用 `mock.patch.dict(sys.modules, ...)` 注入 fake，无需真装 |
| Stage A2 删除 mtca-server.py 后 imports 找不到 | 先 git mv 不存在，用 Remove-Item + git add -u 处理删除 |
| Stage VERIFY 跑 pip install 失败（路径 / 权限） | 报错立即停，把 stderr 完整贴给用户 |
| TDD 测试在 A1 跑失败但 import 报错不是断言错 | 改用 `if importlib` 而不是 `import mtca_server` 直接 import |

## 验收清单（M1 P0 收尾）

| 项 | 指标 | 状态 |
|---|---|---|
| A1 凭证硬编码 | grep 0 行 |  |
| A2 mtca-server 命令可启动 | mtca-server --help 返回 3 子命令 |  |
| A3 setup.py 不存在 | Test-Path 返回 False |  |
| 测试无回归 | 298 passed |  |
| 3 个独立 commit | git log 看到 T_A1/T_A2/T_A3 |  |

完成后 M1 验收的"安装门槛"指标终于能从 ⚠️ 未跑验收 → ✅ 已跑通过。
