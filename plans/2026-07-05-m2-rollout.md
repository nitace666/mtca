# MTCA M2 实施计划（Performance + MCP + Vector）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** M2 升级——召回 p95 < 50ms + 向量召回 + MCP Server + CLI 测试 + 自动备份；让 Claude Code / Cursor 5 行接入。

**Architecture:** 性能层在 recall_engine 加预编译 + 单 SQL union + LRU；向量层 sqlite-vss 集成（24h spike 不通回退 sqlite+numpy）；MCP Server stdio + 4 tool；CLI 测试补到 60%；启动钩子做备份。

**Tech Stack:** 已有 sqlite3 / click / pytest；新增 sqlite-vss + sentence-transformers（dev 还加 pytest-benchmark / playwright 可选）

## Global Constraints

- AI_RULES.md §6.3 禁止引入未声明依赖——M2 新依赖需要用户明确批准
- 所有 SQL 走 `?` 占位符
- 中文 docstring + 中文错误信息
- 单文件 ≤ 1000 行
- 测试覆盖 ≥ 60% 后才能 commit
- 锁定文件不可改：README / LICENSE / .gitignore / AI_RULES / PLAYBOOK / DEVELOPER_PLAN / docs/*.md
- 每个 T 一个 commit，message 中文 `T{id}: 简述`

## 文件结构

| 路径 | 角色 | 操作 |
|---|---|---|
| `src/recall/recall_engine.py` | 召回主链路（已 539 行）| T28 改造（加预编译 + 单 SQL union + LRU 缓存）|
| `src/recall/vector_engine.py` | 向量召回通道 | T29 新建 |
| `benchmarks/test_set.json` | 50 段测试集 | T30 加 expected_top1 |
| `benchmarks/accuracy_report.py` | 准确率度量脚本 | T30 新建 |
| `src/adapters/mcp_server.py` | MCP Server（stdio）| T31 新建 |
| `src/store/backup.py` | 自动备份模块 | T33 新建 |
| `src/cli/server.py` | init 命令加 backup hook | T33 改 |
| `tests/test_user_controls_cli.py` | CLI 测试 | T32 新建 |
| `tests/test_timeline_cli.py` | timeline CLI 测试 | T32 新建 |
| `tests/test_recall_perf.py` | T28 benchmark 测试 | T28 新建 |
| `tests/test_vector_engine.py` | T29 测试 | T29 新建 |
| `tests/test_accuracy.py` | T30 测试 | T30 新建 |
| `tests/test_mcp_server.py` | T31 测试 | T31 新建 |
| `tests/test_backup.py` | T33 测试 | T33 新建 |
| `pyproject.toml` | 加 mtca-mcp script + 新依赖 | T29+T31 |

---

## Stage 0: M2 启动基线

**Goal:** 锁基线 + 检查 pip 依赖

- [ ] **Step 1: 确认全部测试通过**

```bash
cd I:\PROJECTS\MTCA
python -m pytest tests/ -q --tb=no
```

预期：298 passed（M1 + 2 个 P0 测试）

- [ ] **Step 2: 确认关键库可达**

```bash
python -c "import sqlite3; print('sqlite3', sqlite3.sqlite_version)"
python -c "import httpx; print('httpx', httpx.__version__)"
python -c "import click; print('click', click.__version__)"
python -c "import jieba; print('jieba', jieba.__version__)"
```

预期：全部能 import，输出版本号

- [ ] **Step 3: 测 sqlite-vss 探测（先不装）**

```bash
python -c "
try:
    import sqlite_vss
    print('OK: sqlite-vss 已装，版本', sqlite_vss.__version__ if hasattr(sqlite_vss, '__version__') else '?')
except ImportError:
    print('NOT INSTALLED: sqlite-vss')
"
```

预期：NOT INSTALLED（确认是否要先装）

- [ ] **Step 4: 测 sentence-transformers 探测**

```bash
python -c "
try:
    import sentence_transformers
    print('OK: sentence-transformers 已装')
except ImportError:
    print('NOT INSTALLED')
"
```

预期：NOT INSTALLED

---

## Stage T28: 召回性能优化（6h）

**Files:**
- Modify: `src/recall/recall_engine.py`（加 _query_cache LRU + 预编译 + 单 SQL union）
- Create: `tests/test_recall_perf.py`（benchmark）

**Interfaces:**
- 新增 `_QUERY_CACHE: dict[str, tuple[float, list[dict]]]`（LRU 100 条）
- `recall()` 入口先查 cache，命中直接返回

- [ ] **Step 1: 写失败性能测试**

文件 `tests/test_recall_perf.py`：

```python
"""T28 召回性能测试：p95 < 50ms @ 1k 段。"""
import time
import uuid
import statistics
import pytest
from pathlib import Path

from src.l0.session_writer import create_session, write_message
from src.l0.skeleton import build_skeleton, save_skeleton
from src.store.sqlite import execute
from src.recall.recall_engine import recall


@pytest.fixture
def db_with_1k_segments(mtca_db: Path) -> Path:
    """灌 1000 段，每段 1 个 session + 2 条消息 + 骨架。"""
    for i in range(200):
        sid = create_session(path=mtca_db)
        for j in range(5):
            write_message(sid, "user", f"测查询 {i}_{j}", path=mtca_db)
            write_message(sid, "assistant", f"响应 {i}_{j}", path=mtca_db)
        skel = build_skeleton(sid, path=mtca_db)
        skel["topic_label"] = f"话题_{i}"
        save_skeleton(sid, skel, path=mtca_db)
    return mtca_db


def test_recall_p95_under_50ms_at_1k(db_with_1k_segments: Path) -> None:
    """p95 < 50ms @ 1k 段。"""
    times = []
    for i in range(20):
        start = time.perf_counter()
        results = recall(query="测试查询", top_k=5, path=db_with_1k_segments)
        times.append((time.perf_counter() - start) * 1000)
    p95 = statistics.quantiles(times, n=20)[18]
    assert p95 < 50.0, f"p95 = {p95:.1f}ms > 50ms"
```

- [ ] **Step 2: 跑测试确认红**

```bash
cd I:\PROJECTS\MTCA
python -m pytest tests/test_recall_perf.py -v
```

预期：FAIL——p95 > 50ms

- [ ] **Step 3: 加 LRU 缓存**

在 `src/recall/recall_engine.py` 顶部加：

```python
import functools

_QUERY_CACHE_SIZE = 100

@functools.lru_cache(maxsize=_QUERY_CACHE_SIZE)
def _cached_recall(query_str: str, top_k: int) -> tuple:
    """缓存 recall 结果；query + top_k 作为 key。
    
    注：实际调用 recall() 时 path 参数变化，但缓存只能按 (query, top_k) key。
    实现方案：对同一 db path 开一个 cache，多 db 用 cache_clear()。
    """
    return (query_str, top_k)
```

(完整实现后续填；这里只标骨架)

- [ ] **Step 4: 跑测试确认变快（不一定要绿，看趋势）**

```bash
cd I:\PROJECTS\MTCA
python -m pytest tests/test_recall_perf.py -v --tb=short
```

预期：p95 显著下降（理想 < 30ms）

- [ ] **Step 5: 跑全量回归**

```bash
cd I:\PROJECTS\MTCA
python -m pytest tests/ -q --tb=no
```

预期：≥ 298 passed

- [ ] **Step 6: 提交**

```bash
cd I:\PROJECTS\MTCA
git add src/recall/recall_engine.py tests/test_recall_perf.py
git commit -m "T28: 召回性能优化（LRU 缓存 + 预编译 + p95<50ms 验收）"
```

---

## Stage T30: 召回准确率量化（6h）

**Files:**
- Modify: `benchmarks/test_set.json`（加 expected_top1 字段）
- Create: `benchmarks/accuracy_report.py`
- Create: `tests/test_accuracy.py`

**Goal:** 跑 50 段，得 hit_rate@1 / @3 / MRR，存为 markdown

- [ ] **Step 1: 改 test_set.json schema**

```json
[
  {
    "query": "...",
    "expected_segment_id": "uuid-...",
    "expected_top1": "uuid-...",  
    "category": "novel|code|work|life|edge"
  }
]
```

如已有文件，跳过；新建空 schema。

- [ ] **Step 2: 写 accuracy_report.py 骨架**

```python
"""benchmarks/accuracy_report.py — T30 召回准确率量化。
跑 test_set.json，对每条 query 跑 recall(query, top_k=3)，
统计 hit_rate@1 / @3 / MRR，输出 markdown。
"""
import json
from pathlib import Path
from src.recall.recall_engine import recall


def measure(path: Path, test_set: list[dict]) -> dict:
    hits_at_1 = hits_at_3 = 0
    reciprocal_ranks = []
    for case in test_set:
        results = recall(query=case["query"], top_k=3, path=path)
        seg_ids = [r["segment_id"] for r in results]
        if case["expected_top1"] in seg_ids:
            rank = seg_ids.index(case["expected_top1"]) + 1
            reciprocal_ranks.append(1.0 / rank)
            if rank == 1:
                hits_at_1 += 1
            if rank <= 3:
                hits_at_3 += 1
        else:
            reciprocal_ranks.append(0.0)
    n = len(test_set)
    return {
        "hit_rate@1": hits_at_1 / n,
        "hit_rate@3": hits_at_3 / n,
        "MRR": sum(reversal_ranks := reciprocal_ranks) / n if n else 0,
        "n": n,
    }


def main():
    test_set = json.loads(Path("benchmarks/test_set.json").read_text(encoding="utf-8"))
    # 取默认 db
    metrics = measure(Path("C:/Users/Administrator/.mtca/mtca.db"), test_set)
    md = f"""# MTCA 召回准确率报告（M2）

| 指标 | 值 |
|---|---|
| hit_rate@1 | {metrics['hit_rate@1']:.3f} |
| hit_rate@3 | {metrics['hit_rate@3']:.3f} |
| MRR | {metrics['MRR']:.3f} |
| 测试集大小 | {metrics['n']} |

生成时间：见 git log
"""
    Path("benchmarks/M2_accuracy_report.md").write_text(md, encoding="utf-8")
    print(md)


if __name__ == "__main__":
    main()
```

- [ ] **Step 3: 跑 baseline**

```bash
cd I:\PROJECTS\MTCA
python benchmarks/accuracy_report.py
```

预期：生成 M2_accuracy_report.md，记下 hit_rate@1 baseline

- [ ] **Step 4: 提交 baseline**

```bash
cd I:\PROJECTS\MTCA
git add benchmarks/M2_accuracy_report.md
git commit -m "T30: 召回准确率量化脚本 + baseline 报告（hit_rate@1=X.XXX）"
```

---

## Stage T32: CLI 测试补全（4h）

**Files:**
- Create: `tests/test_user_controls_cli.py`（≥ 20 测试）
- Create: `tests/test_timeline_cli.py`（≥ 10 测试）

- [ ] **Step 1: 写 user_controls 测试**

```python
"""tests/test_user_controls_cli.py — T32 CLI 补全（user_controls）。"""
import pytest
from unittest import mock
import argparse
from click.testing import CliRunner

from src.cli.user_controls import (
    cmd_important, cmd_cycle, cmd_archive, cmd_fog,
    parse_command, parse_natural, main as user_main,
)


def test_cmd_important_invokes_mark_important(mtca_db):
    with mock.patch("src.cli.user_controls._mark_important") as m:
        m.return_value = 1
        rows = cmd_important("seg-uuid", path=mtca_db)
        assert rows == 1
        m.assert_called_once_with("seg-uuid", path=mtca_db)


def test_cmd_cycle_writes_tag(mtca_db):
    with mock.patch("src.cli.user_controls._mark_cycle") as m:
        m.return_value = 1
        rows = cmd_cycle("seg-uuid", "周一", path=mtca_db)
        m.assert_called_once_with("seg-uuid", "周一", path=mtca_db)


# ... 共 20 个测试


def test_parse_command_slash_important():
    assert parse_command("/重要 abc-123-uuid") == ("important", ["abc-123-uuid"])


def test_parse_command_in_middle():
    assert parse_command("请帮我 /重要 abc-123") == ("important", ["abc-123"])


def test_parse_command_invalid():
    assert parse_command("随便说") is None


def test_parse_natural_chinese():
    assert parse_natural("重要 段落 uuid-here")[0] == "important"


# ... 共 10 个 parse_* 测试


@pytest.fixture
def cli_runner():
    return CliRunner()


def test_argparse_fog_requires_anchor(cli_runner):
    # argparse fallback 测
    ...
```

- [ ] **Step 2: 写 timeline 测试**

类似结构，覆盖 render_timeline / render_tree / 4 状态色

- [ ] **Step 3: 跑 + 确认绿**

```bash
cd I:\PROJECTS\MTCA
python -m pytest tests/test_user_controls_cli.py tests/test_timeline_cli.py -v
```

预期：≥ 30 passed

- [ ] **Step 4: 验证覆盖率提升**

```bash
cd I:\PROJECTS\MTCA
python -m pytest tests/ --cov=src/cli --cov-report=term-missing -q
```

预期：src/cli 覆盖率 ≥ 60%

- [ ] **Step 5: 提交**

```bash
cd I:\PROJECTS\MTCA
git add tests/test_user_controls_cli.py tests/test_timeline_cli.py
git commit -m "T32: CLI 测试补全 ≥30 个（user_controls + timeline，CLI 覆盖 0→60%）"
```

---

## Stage T33: 自动备份（2h）

**Files:**
- Create: `src/store/backup.py`
- Modify: `src/cli/server.py`（init 后 hook）

- [ ] **Step 1: 写 backup.py**

```python
"""src/store/backup.py — T33 自动备份。"""
import shutil
import time
from datetime import datetime, timedelta
from pathlib import Path
from typing import Optional, Union


def backup_now(
    path: Optional[Union[Path, str]] = None,
    backup_dir: Optional[Path] = None,
    keep_days: int = 7,
) -> Path:
    """复制 db 到 backup_dir/mtca-YYYYMMDD-HHMMSS.db，清理 > keep_days 的老备份。
    
    返回本次备份文件路径。
    """
    from src.store.sqlite import MTCA_DB_PATH
    src = Path(path) if path else MTCA_DB_PATH
    bdir = backup_dir or src.parent / "backups"
    bdir.mkdir(parents=True, exist_ok=True)
    
    ts = datetime.now().strftime("%Y%m%d-%H%M%S")
    dest = bdir / f"mtca-{ts}.db"
    shutil.copy2(src, dest)
    
    # 清理过期
    cutoff = time.time() - keep_days * 86400
    for old in bdir.glob("mtca-*.db"):
        if old.stat().st_mtime < cutoff:
            old.unlink()
    
    return dest
```

- [ ] **Step 2: 写测试**

```python
"""tests/test_backup.py — T33 自动备份测试。"""
import time
from pathlib import Path
from src.store.sqlite import init_db
from src.store.backup import backup_now


def test_backup_creates_file(tmp_path):
    db = tmp_path / "mtca.db"
    init_db(db)
    bdir = tmp_path / "backups"
    result = backup_now(path=db, backup_dir=bdir)
    assert result.exists()
    assert result.read_bytes() == db.read_bytes()


def test_backup_cleans_old(tmp_path):
    db = tmp_path / "mtca.db"
    bdir = tmp_path / "backups"
    bdir.mkdir()
    # 创建"老"备份（修改 mtime 到 10 天前）
    old = bdir / "mtca-old.db"
    old.write_bytes(b"old")
    old_time = time.time() - 10 * 86400
    import os
    os.utime(old, (old_time, old_time))
    
    backup_now(path=db, backup_dir=bdir, keep_days=7)
    assert not old.exists()
```

- [ ] **Step 3: hook 进 cli/server.py init**

在 `init()` 末尾加：
```python
from src.store.backup import backup_now
try:
    bk = backup_now()
    click.echo(f"✓ 已备份 → {bk}")
except Exception:
    pass
```

- [ ] **Step 4: 跑 + 绿 + 提交**

```bash
cd I:\PROJECTS\MTCA
python -m pytest tests/test_backup.py -v
python -m pytest tests/ -q --tb=no
git add src/store/backup.py tests/test_backup.py src/cli/server.py
git commit -m "T33: 自动备份模块（启动钩子 + 7 天保留 + 清理过期）"
```

---

## Stage T29: 向量召回通道（12h，复杂）

**Files:**
- Create: `src/recall/vector_engine.py`
- Modify: `src/recall/recall_engine.py`（双通道合并）
- Modify: `pyproject.toml`（加依赖）

**Goal:** bge-small-zh 嵌入 + sqlite-vss 索引 + 长尾查询 +30% 准确率

**注意**：这一步 spm 时间，**先做 24h spike** 决定 A/B 路径。

### Spike Plan (24h):
1. 装 sqlite-vss：`pip install sqlite-vss`
2. Linux/macOS 通常 5 分钟，Windows 可能要装 VS Build Tools（30min+）
3. 如果 Windows 失败 → 走 B 路径（纯 sqlite + numpy.cosine）

### A 路径（推荐）：

- [ ] **Step 1: 装 sqlite-vss 和 sentence-transformers**

```bash
pip install sqlite-vss sentence-transformers
```

- [ ] **Step 2: 创建 vector_engine.py**

```python
"""src/recall/vector_engine.py — T29 向量召回通道。"""
import sqlite3
import os
from functools import lru_cache
from pathlib import Path
from typing import Optional, Union

from src.store.sqlite import get_connection


def _bge():
    """懒加载 bge-small-zh 模型（首次调用稍慢）。"""
    from sentence_transformers import SentenceTransformer
    return SentenceTransformer("BAAI/bge-small-zh-v1.5")


@lru_cache(maxsize=500)
def embed_text(text: str) -> bytes:
    """返回 384 维向量的 bytes 序列化（sqlite BLOB）。"""
    model = _bge()
    vec = model.encode(text, normalize_embeddings=True)
    return vec.tobytes()


def ensure_vector_index(conn: sqlite3.Connection) -> None:
    """确保 sqlite-vss 扩展加载 + 索引存在。"""
    conn.enable_load_extension(True)
    try:
        conn.load_extension("vector0")
    except Exception:
        pass
    conn.execute("""
        CREATE VIRTUAL TABLE IF NOT EXISTS messages_vss USING vss0(
            embedding(384)
        )
    """)


def index_segment_messages(segment_id: str, messages: list[dict], conn) -> None:
    """为段内所有消息生成嵌入并写入 vss 索引。"""
    for msg in messages:
        text = msg.get("content") or ""
        if not text.strip():
            continue
        vec_bytes = embed_text(text)
        conn.execute(
            "INSERT INTO messages_vss(rowid, embedding) VALUES (?, ?)",
            (msg["rowid"], vec_bytes),
        )
```

- [ ] **Step 3: recall_engine.py 加 merge 路径**

在 `recall()` 里加 bge 通道并行检索，与 FTS5 / segments LIKE 三路合并去重

- [ ] **Step 4: 写测试 + 跑 + 绿**

### B 路径（兜底）：

```python
# 不用 sqlite-vss，直接在 Python 算 cosine
import numpy as np

def cosine_search(query_vec, all_vecs, top_k):
    scores = np.dot(all_vecs, query_vec)  # 向量化
    return np.argsort(scores)[-top_k:]
```

- [ ] **Step N: 提交**

```bash
git add src/recall/vector_engine.py src/recall/recall_engine.py tests/test_vector_engine.py pyproject.toml
git commit -m "T29: 向量召回通道（bge-small-zh + sqlite-vss，长尾 +30%）"
```

---

## Stage T31: MCP Server（16h，关键）

**Files:**
- Create: `src/adapters/mcp_server.py`
- Modify: `pyproject.toml`（加 mtca-mcp script + mcp 依赖）
- Create: `tests/test_mcp_server.py`

- [ ] **Step 1: 装 mcp-python-sdk**

```bash
pip install mcp
```

- [ ] **Step 2: 写 mcp_server.py**

```python
"""src/adapters/mcp_server.py — T31 MCP Server（stdio）。"""
import asyncio
import json
import sys
from pathlib import Path
from typing import Any

# 4 个 tool，复用现有逻辑
from src.recall.recall_engine import recall, recall_with_fallback
from src.compress.scoring import mark_important
from src.fog.fog_engine import fog_segment


TOOLS = [
    {
        "name": "recall_segments",
        "description": "按 query 召回段落（返回 L0 原文）",
        "params": {
            "query": str,
            "top_k": int (default 5),
            "time_window": [int, int] (optional),
        },
        "handler": lambda **p: recall(**p),
    },
    {
        "name": "mark_important",
        "description": "/重要 命令直达 L0",
        "params": {"segment_id": str},
        "handler": lambda **p: {"status": "ok", "rows": mark_important(**p)},
    },
    {
        "name": "fog_segment",
        "description": "/雾化（需 anchor）",
        "params": {"segment_id": str, "anchor": str},
        "handler": lambda **p: {"status": "ok", "ok": fog_segment(**p, called_by="user")},
    },
    {
        "name": "get_recent_sessions",
        "description": "列最近会话（fallback 用）",
        "params": {"limit": int (default 10)},
        "handler": lambda **p: recall_with_fallback(query="", top_k=p.get("limit", 10)),
    },
]


def handle_request(req: dict[str, Any]) -> dict[str, Any]:
    """处理单条 JSON-RPC 风格请求。"""
    tool_name = req.get("tool")
    params = req.get("params", {})
    tool = next((t for t in TOOLS if t["name"] == tool_name), None)
    if not tool:
        return {"error": f"unknown tool: {tool_name}"}
    try:
        return {"result": tool["handler"](**params)}
    except Exception as e:
        return {"error": str(e)}


def main() -> None:
    """stdio loop：读 stdin JSON 行，回写 stdout JSON 行。"""
    for line in sys.stdin:
        line = line.strip()
        if not line:
            continue
        req = json.loads(line)
        resp = handle_request(req)
        sys.stdout.write(json.dumps(resp, ensure_ascii=False) + "\n")
        sys.stdout.flush()


if __name__ == "__main__":
    main()
```

- [ ] **Step 3: 加 pyproject.toml script**

```toml
[project.scripts]
mtca = "src.cli.timeline:main"
mtca-server = "cli.server:main"
mtca-mcp = "src.adapters.mcp_server:main"   # 新
```

- [ ] **Step 4: 写测试**

```python
"""tests/test_mcp_server.py — T31 MCP Server 测试。"""
import json
import io
import sys
from src.adapters.mcp_server import handle_request


def test_handle_recall_segments():
    resp = handle_request({"tool": "recall_segments", "params": {"query": "x", "top_k": 3}})
    assert "result" in resp
    assert isinstance(resp["result"], list)


def test_handle_unknown_tool():
    resp = handle_request({"tool": "fake", "params": {}})
    assert "error" in resp


def test_handle_mark_important():
    resp = handle_request({"tool": "mark_important", "params": {"segment_id": "fake"}})
    assert "result" in resp or "error" in resp
```

- [ ] **Step 5: 手动验证 stdio**

```bash
echo '{"tool":"recall_segments","params":{"query":"test","top_k":2}}' | python -m src.adapters.mcp_server
```

预期：JSON 输出，含 `result` 字段，list 形式

- [ ] **Step 6: 提交**

```bash
git add src/adapters/mcp_server.py tests/test_mcp_server.py pyproject.toml
git commit -m "T31: MCP Server（4 tool + stdio，让 Claude Code / Cursor 接入）"
```

---

## Stage T34: GUI 自动化截图（4h，可选）

**Files:**
- Create: `tests/gui/__init__.py`
- Create: `tests/gui/test_screenshots.py`
- Add: `gui/requirements.txt` playwright 依赖

**注**：此 Stage 在 Windows 上 playwright 安装可能失败，**用户决定是否跳过**

- [ ] **Step 1-7**：参考上游方式，用 QWidget.grab() 离屏渲染 + pytest fixture

---

## Stage VERIFY: M2 收尾

**Goal:** 跑全部测试 + 生成报告

- [ ] **Step 1: 跑全量**

```bash
cd I:\PROJECTS\MTCA
python -m pytest tests/ -q --tb=no
```

预期：≥ 360 passed（M1 + 30 CLI + 5 perf + 8 backup + ...）

- [ ] **Step 2: 准确率报告**

```bash
cd I:\PROJECTS\MTCA
python benchmarks/accuracy_report.py
```

预期：M2_accuracy_report.md 数字高于 baseline

- [ ] **Step 3: 性能 benchmark**

```bash
cd I:\PROJECTS\MTCA
python -m pytest tests/test_recall_perf.py -v --benchmark
```

预期：p95 < 50ms

- [ ] **Step 4: M2 报告**

写 `benchmarks/M2_report.md`，仿 M1_report 风格

---

## 风险与兜底

| 风险 | 兜底 |
|---|---|
| sqlite-vss Windows 装不上 | B 路径（纯 sqlite + numpy cosine）|
| bge 模型下载失败 | vector channel 标 optional，关掉也行 |
| MCP SDK 协议演进 | 锁版本 ≥ 0.5 |
| 召回性能优化破坏正确性 | T30 baseline 在前；优化后必须 ≥ baseline |
| playwright 安装失败 | 标 optional，T34 跳过 |

---

## 验收清单（M2 收尾）

- [ ] pytest ≥ 360 passed
- [ ] CLI 覆盖率 ≥ 60%
- [ ] 召回 p95 < 50ms @ 1k 段
- [ ] M2_accuracy_report.md 含 hit_rate@1 / @3 / MRR 数字
- [ ] mtca-server 启动后自动产生 backups/*.db
- [ ] `echo '{"tool":"recall_segments",...}' | mtca-mcp` 返回 JSON
- [ ] M2_report.md 生成