# MTCA M2.5 动态评分方案（v0.5）

> **副标题**：从"数据库式记忆"升级到"人脑式记忆"
> **作者**：老板（产品决策）+ AI 协作
> **状态**：M2.5 启动（设计锁定中）
> **位置**：`./`
> **依赖**：M1 已完成（21/28 T 任务 + 8 个 M2 增强悄悄做完）
> **下一步**：M2 性能 + MCP + 向量召回 → M2.5 动态评分 → M3 跨设备 + 主动智能

---

## 0. 为什么是 M2.5（不塞 M2 / 也不推迟到 M3）

| 里程碑 | 内容 | 工时 | 优先级 |
|---|---|---|---|
| **M2** | 召回性能优化（145ms→<50ms）+ MCP Server + sqlite-vss 向量召回 | ~80h | 🔴 必做 |
| **M2.5**（本文件）| 4 象限 + 多因子打分 + 紧急追踪 + 情感属性 | ~120h | 🔴 必做 |
| **M3** | CRDT 跨设备同步 + 主动智能 + Web 时间线 | ~80h | 🟡 M2.5 后做 |

**为什么独立成 M2.5**：
- M2 焦点是**底层性能 + 外部接入**——MCP/向量召回是"通向外部"
- M2.5 焦点是**评分模型智能化**——4 象限/紧急/情感是"理解用户"
- M3 焦点是**跨设备 + 主动**——CRDT/主动智能是"协同 + 预测"
- 混在一起做，每块都做不完，3 块都废

**M2.5 的核心赌注**：MTCA 不再是"对话存档 + 全文搜索"，而是"**真正懂你忙什么、担心什么、什么不能忘**"的助理。

---

## 1. 产品哲学（新增第 9 条铁律）

### 1.1 你的 3 个核心需求（来自老板原话）

> 1. **首先长期使用节省 token**——不要让 MTCA 变成"另一个 OpenAI 账单"
> 2. **AI 必须保持长期记忆**——让它真正作为助理存在，**不是一次性智能体**
> 3. **接受 AI 犯错，但决不接受遗忘重要**——犯错可以查 L0 挽救，遗忘了就什么都没了

### 1.2 9 条铁律（M2.5 新增第 9 条）

1. **数据主权在用户**——本地 SQLite
2. **L0 双层 + AI 不可变**——骨架 / 细节分层
3. **L1–L3 是视图**——可重建、可损失
4. **雾化 = 用户主动丢弃细节**——不可逆
5. **召回错误自动回 L0-骨架**——摘要层丢失的兜底
6. **用户控制接口直达 L0**——`/重要 /循环 /归档 /雾化`
7. **GUI 优先**——时间线 / 树状 / 雾化 / 紧急 / 4 象限全在 GUI
8. **存储成本由用户承担**——1-2 GB/年可忽略
9. 🆕 **重要记忆永不遗忘**——AI 可犯技术错误（可回 L0 挽救），**但不得让"重要 / 紧急 / 用户强调时效"段下沉到不可召回档**

> **第 9 铁律是 M2.5 的灵魂**——它把"token 节省"和"绝不遗忘"统一起来：
> - 不重要的（Q4）3 天就压缩成 L2 → 省 token
> - 重要的（Q1/Q2）永不衰减 → 不遗忘
> - 用户强调的（urgent_state='tracking'）过期必问 → 既不遗忘也不浪费

---

## 2. 4 象限分类 + 已强调时效

### 2.1 二维空间定义

每个段落在 (urgency, importance) 二维空间里有一个点：

| 维度 | 值域 | 默认 | 含义 |
|---|---|---|---|
| `urgency_level` | 0.0-1.0 | 0.5 | 时效压力：高 = 急 / 低 = 缓 |
| `importance_level` | 0.0-1.0 | 0.5 | 长期价值：高 = 重要 / 低 = 闲聊 |

### 2.2 4 象限 + 1 特殊类

| 类别 | importance | urgency | half_life | 衰减后 | 过期询问 | 例子 |
|---|---|---|---|---|---|---|
| **Q1** 重要+紧急 | ≥0.7 | ≥0.7 | 180d | dormant（永不 silent）| ✅ | "明天的客户演示" |
| **Q2** 重要+不紧急 | ≥0.7 | <0.7 | 365d | silent | ❌ | "3 年前的学习笔记" |
| **Q3** 不重要+紧急 | <0.7 | ≥0.7 | 14d | silent | ❌ | "今天天气" |
| **Q4** 都不沾边 | <0.7 | <0.7 | 3d | silent | ❌ | "闲聊" |
| 🆕 **URGENT 已强调时效** | 任意 | 任意 | **∞**（不衰减）| dormant | ✅ 过期必问 | "周一前完成 / 尽快" |

**关键约束**：
- Q1 段 + 已 /重要 段 + URGENT 段 = **3 类永不遗忘**（满足第 9 铁律）
- 旧数据自动归到 (0.5, 0.5) → 走 Q2/Q3 边界策略
- 用户可手动调高 / 调低 urgency / importance

### 2.3 为什么是这 4 个象限（不是 5 个或 3 个）

来自经典的 Eisenhower 矩阵：
- 重要×紧急 = 4 象限全覆盖
- 加 1 个"用户显式强调时效"独立类 = 因为它的衰减行为完全不同（不衰减）
- 没加"工作 / 生活"分类 = 因为是 category 维度，不是 priority 维度，应该走 topic_label

---

## 3. Schema 扩展（T29，向后兼容）

### 3.1 新增字段（ALTER TABLE）

```sql
-- 全部 DEFAULT，旧数据自动用默认值；幂等可重跑
ALTER TABLE segments ADD COLUMN urgency_level     REAL    DEFAULT 0.5;
ALTER TABLE segments ADD COLUMN importance_level  REAL    DEFAULT 0.5;
ALTER TABLE segments ADD COLUMN emotion_tag       TEXT    DEFAULT NULL;
ALTER TABLE segments ADD COLUMN expires_at_ms     INTEGER DEFAULT NULL;
ALTER TABLE segments ADD COLUMN urgent_state      TEXT    DEFAULT NULL;
-- 索引（urgent_state 是热点查询）
CREATE INDEX IF NOT EXISTS idx_segments_urgent
    ON segments(urgent_state, expires_at_ms)
    WHERE urgent_state IS NOT NULL;
```

### 3.2 关键约束（不改 8 铁律）

| 字段 | L0-骨架保护？ | AI 可改？ | 备注 |
|---|---|---|---|
| `urgency_level` | ❌ 不在白名单 | ❌（需走 update_segment）| 系统字段 |
| `importance_level` | ❌ | ❌ | 系统字段 |
| `emotion_tag` | ❌ | ❌ | 系统字段 |
| `expires_at_ms` | ❌ | ❌ | 系统字段 |
| `urgent_state` | ❌ | ❌ | 系统字段 |

`no_update_skeleton` 触发器锁的 3 个字段（`topic_label / start_at / fog_anchor`）**继续保护**。新字段是系统自动维护的"调度字段"，与 `current_score / current_tier / fog_state` 同等待遇——**AI 不能直接改，必须走 `update_segment()` 函数**。

### 3.3 `_UPDATABLE_FIELDS` 白名单扩展

```python
# src/l0/segment_writer.py 第 48 行附近
_UPDATABLE_FIELDS: frozenset[str] = frozenset({
    # ... 原有字段
    "urgency_level", "importance_level", "emotion_tag",
    "expires_at_ms", "urgent_state",
})
```

### 3.4 迁移脚本（src/store/sqlite.py 追加）

```python
# init_db() 末尾追加
_MIGRATION_V2_SQL: str = """
ALTER TABLE segments ADD COLUMN urgency_level REAL DEFAULT 0.5;
ALTER TABLE segments ADD COLUMN importance_level REAL DEFAULT 0.5;
ALTER TABLE segments ADD COLUMN emotion_tag TEXT DEFAULT NULL;
ALTER TABLE segments ADD COLUMN expires_at_ms INTEGER DEFAULT NULL;
ALTER TABLE segments ADD COLUMN urgent_state TEXT DEFAULT NULL;
CREATE INDEX IF NOT EXISTS idx_segments_urgent
    ON segments(urgent_state, expires_at_ms)
    WHERE urgent_state IS NOT NULL;
"""

def _migrate_v2(conn: sqlite3.Connection) -> None:
    """M2.5 schema 迁移：幂等（PRAGMA table_info 检查列是否存在）。"""
    cur = conn.execute("PRAGMA table_info(segments)")
    cols = {row[1] for row in cur.fetchall()}
    for stmt in _MIGRATION_V2_SQL.split(";"):
        stmt = stmt.strip()
        if not stmt or stmt.startswith("CREATE INDEX"):
            # 索引单独处理
            try:
                conn.execute(stmt)
            except sqlite3.OperationalError:
                pass  # 索引已存在
            continue
        if stmt.startswith("ALTER TABLE"):
            col_name = stmt.split("ADD COLUMN")[1].split()[0]
            if col_name in cols:
                continue
            conn.execute(stmt)
    conn.commit()
```

---

## 4. 多因子打分公式（T30，替换线性衰减）

### 4.1 当前实现的问题

```python
# 现状（src/compress/scoring.py:303 附近）
new_score = max(0.0, old_score - PER_TICK_DECAY)   # 每轮 -1
# 阈值：L1=70 / L2=50 / L3=30 / L3_hidden<30
```

**问题**：
- 重要 3 年的笔记（Q2）和小猫视频（Q4）衰减速度一样——**违反第 9 铁律**
- 时间维度只看"距上次 promoted_at"——**不区分"距事件发生已多久"**
- 没有"紧急"概念——**所有活跃段一视同仁**

### 4.2 M2.5 公式

```python
# src/compress/scoring.py 替换 tick() 主体

def calculate_score(seg: dict, now_ms: int) -> tuple[float, str]:
    """多因子加权打分（M2.5）。返回 (new_score, new_tier)。"""
    base = max(0.0, float(seg.get("current_score", 100.0)) - PER_TICK_DECAY)
    
    # 4 因子（0.5-1.5x 量级，避免数值爆炸）
    f_urgency    = 0.5 + float(seg.get("urgency_level", 0.5)) * 1.0
    f_importance = 0.3 + float(seg.get("importance_level", 0.5)) * 1.7
    f_emotion    = _emotion_factor(seg.get("emotion_tag"))
    f_reference  = 1.0 + min(int(seg.get("ref_count", 0)), 5) * 0.1
    
    # 时间衰减（指数，按象限配 half_life）
    quadrant = _get_quadrant(seg)
    half_life_days = _HALF_LIFE_DAYS[quadrant]  # Q1=180, Q2=365, Q3=14, Q4=3
    age_days = max(0, (now_ms - int(seg.get("promoted_at", now_ms))) / 86400000)
    f_time = 0.5 ** (age_days / half_life_days)
    
    # 第 9 铁律：URGENT / /重要 段不衰减
    if (seg.get("urgent_state") == "tracking" 
        or float(seg.get("current_score", 0)) >= IMPORTANT_SCORE):
        f_time = 1.0
    
    new_score = base * f_urgency * f_importance * f_emotion * f_reference * f_time
    new_tier = _determine_tier_v2(new_score, quadrant)
    return new_score, new_tier

_HALF_LIFE_DAYS: dict[str, int] = {"Q1": 180, "Q2": 365, "Q3": 14, "Q4": 3}

def _emotion_factor(tag: Optional[str]) -> float:
    return {
        "positive": 1.2, "excited": 1.15, "anxious": 1.1,
        "neutral": 1.0, "negative": 0.8, None: 1.0,
    }.get(tag, 1.0)

def _get_quadrant(seg: dict) -> str:
    urg = float(seg.get("urgency_level", 0.5))
    imp = float(seg.get("importance_level", 0.5))
    if imp >= 0.7 and urg >= 0.7: return "Q1"
    if imp >= 0.7:              return "Q2"
    if urg >= 0.7:              return "Q3"
    return "Q4"

def _determine_tier_v2(score: float, quadrant: str) -> str:
    """Q1 永不进 L3_hidden；其他按阈值。"""
    if quadrant == "Q1" and score < L3_THRESHOLD:
        return "L3"   # 锁底
    if score >= L1_THRESHOLD: return "L1"
    if score >= L2_THRESHOLD: return "L2"
    if score >= L3_THRESHOLD: return "L3"
    return "L3_hidden"
```

### 4.3 公式背后的工程取舍

| 设计 | 取舍 |
|---|---|
| **乘法而非加法** | 因子之间是"加强/削弱"关系，不是"叠加"——A 重要 + B 紧急 比 A+B 重要更显著 |
| **时间指数衰减** | 半年后还在用 = 真的重要；半年后没动 = 真的不重要 |
| **Q1 锁底到 L3** | 即使分数低也不进 L3_hidden——GUI 仍能看到（满足第 9 铁律）|
| **URGENT 强制 f_time=1.0** | 用户说过"急"的段，时间维度不参与衰减 |
| **/重要 同样锁** | 10000 分数 + IMPORT_SCORE 触发同条路径 |

---

## 5. 紧急追踪（T32，urgent_tracker.py）

### 5.1 状态机

```
                  用户说"明天要"
                        ↓
   NULL ──────────────────→ 'tracking'
                                ↓
                          过期时间到
                                ↓
                          'expired'
                          ↙   ↓   ↘
                  'completed'  'postponed'  'important'
                  (压缩 L2)    (延 7d)     (升 /重要)
```

### 5.2 核心 API

```python
# src/lifecycle/urgent_tracker.py（新文件）

def track_urgent(
    segment_id: str,
    expires_at_ms: int,
    anchor: Optional[str] = None,
    path: Optional[Path] = None,
) -> dict:
    """用户说"X 时要这个" → 标记 + 暂停衰减。"""
    update_segment(seg_id, path=path,
        urgency_level=1.0,
        expires_at_ms=expires_at_ms,
        urgent_state="tracking",
    )
    return {"segment_id": seg_id, "expires_at_ms": expires_at_ms, "state": "tracking"}

def tick_urgent(path: Optional[Path] = None, now_ms: Optional[int] = None) -> list[str]:
    """每轮扫：urgent_state='tracking' 且已过期的段 → 转 'expired'。"""
    now = int(now_ms) if now_ms else _now_ms()
    rows = query(
        "SELECT segment_id, topic_label, fog_anchor FROM segments "
        "WHERE urgent_state = 'tracking' AND expires_at_ms <= ?",
        (now,), path=path,
    )
    expired_ids = []
    for r in rows:
        update_segment(r["segment_id"], path=path, urgent_state="expired")
        expired_ids.append(r["segment_id"])
    return expired_ids

def handle_urgent_response(
    segment_id: str,
    response: str,  # "completed" / "postponed" / "important"
    path: Optional[Path] = None,
    now_ms: Optional[int] = None,
) -> dict:
    """用户对 expired 段的响应。
    - completed: 压缩到 L2（直接降 tier，fog_state 不动）
    - postponed: 重置 expires_at_ms = now + 7d，urgent_state 回 tracking
    - important: 调 mark_important()，current_score=10000
    """
    norm = response.strip().lower()
    if norm == "completed":
        update_segment(seg_id, path=path,
            current_tier="L2",
            urgent_state="completed",
        )
        # 触发 L2 摘要重写（异步）
        schedule_regen(seg_id, reason="fog", path=path)
    elif norm == "postponed":
        new_expires = (int(now_ms) if now_ms else _now_ms()) + 7 * 86400 * 1000
        update_segment(seg_id, path=path,
            expires_at_ms=new_expires,
            urgent_state="tracking",
        )
    elif norm == "important":
        mark_important(seg_id, path=path)
    else:
        raise ValueError(f"response 必须是 completed/postponed/important，收到：{response!r}")
    return {"segment_id": seg_id, "new_state": norm}
```

### 5.3 集成点

```python
# 1. scoring.tick() 末尾：tick_urgent() 一起跑
def tick(path=None, ...):
    n_scored = _score_active_segments(path=path)
    n_expired = tick_urgent(path=path)
    return n_scored + len(n_expired)   # 返回 dict 更明确

# 2. GUI：expired 段在 GUI 顶部红条显示
# gui/main_window.py 加一个 QListWidget 展示 expired 列表
# 双击 → 弹对话框（3 按钮：完成 / 延期 / 重要）

# 3. CLI：mtca urgent-check 命令
# src/cli/user_controls.py 加 subcommand
```

---

## 6. 情感属性（T31，skeleton.py 扩展）

### 6.1 数据流

```
write_message() → build_skeleton() → 异步 extract_emotion()
                                              ↓
                              update_segment(emotion_tag=...)
```

### 6.2 双路径（LLM 优先，关键词 fallback）

```python
# src/l0/skeleton.py 新增

_EMOTION_KEYWORDS: dict[str, set[str]] = {
    "positive": {"开心", "成功", "完成", "棒", "满意", "happy", "great", "done", "✓"},
    "excited":  {"兴奋", "期待", "激动", "迫不及待", "excited", "eager"},
    "anxious":  {"担心", "焦虑", "急", "赶", "ddl", "worry", "urgent", "rush"},
    "negative": {"失败", "错了", "糟", "失望", "崩", "fail", "bad", "wrong", "崩溃"},
    # 默认 neutral
}

def extract_emotion(messages: list[dict], provider: Any = None) -> str:
    """轻量情感判断。LLM 优先，失败用关键词。"""
    text = " ".join(m.get("content") or "" for m in messages)
    if not text.strip():
        return "neutral"
    
    # 1. LLM 路径（可选）
    if provider is not None and provider.is_available():
        try:
            prompt = ("判断下面文字的情感极性。"
                      "仅输出：positive/excited/anxious/negative/neutral\n" + text[:300])
            raw = provider.generate(prompt, max_tokens=10)
            return _normalize_emotion(raw)
        except Exception:
            pass  # 降级到关键词
    
    # 2. 关键词 fallback（无 LLM 也跑通）
    return _emotion_keyword_match(text)

def _emotion_keyword_match(text: str) -> str:
    text_lower = text.lower()
    scores: dict[str, int] = {k: 0 for k in _EMOTION_KEYWORDS}
    for emotion, kws in _EMOTION_KEYWORDS.items():
        for kw in kws:
            if kw.lower() in text_lower:
                scores[emotion] += 1
    if max(scores.values()) == 0:
        return "neutral"
    return max(scores, key=scores.get)

def _normalize_emotion(raw: str) -> str:
    raw = raw.strip().lower()
    aliases = {"好": "positive", "坏": "negative", "急": "anxious",
               "开心": "positive", "担心": "anxious", "一般": "neutral"}
    return aliases.get(raw, raw if raw in {"positive","excited","anxious","negative","neutral"} else "neutral")
```

### 6.3 异步执行

不在 `write_message` 主链路里跑——挂后台线程（参考 `regen_engine.start_background_worker`）：

```python
# src/l0/skeleton.py
_emotion_queue: queue.Queue = queue.Queue()

def schedule_emotion_extraction(segment_id: str, messages: list[dict]) -> None:
    """入队，异步跑 LLM 提取。"""
    _emotion_queue.put({"segment_id": segment_id, "messages": messages})

def start_emotion_worker(provider: Any = None, path: Optional[Path] = None) -> Thread:
    """启动后台 worker，每 200ms 检查队列。"""
    def _loop():
        while True:
            try:
                item = _emotion_queue.get(timeout=0.2)
                emo = extract_emotion(item["messages"], provider=provider)
                update_segment(item["segment_id"], path=path, emotion_tag=emo)
            except queue.Empty:
                continue
            except Exception:
                pass  # 失败不阻塞
    t = Thread(target=_loop, daemon=True)
    t.start()
    return t
```

---

## 7. CLI 3 命令（T33，user_controls.py 扩展）

### 7.1 新增命令

| 命令 | 别名 | 行为 |
|---|---|---|
| `/紧急 <seg_id> <时间>` | `/urgent` | track_urgent(seg_id, expires_at_ms) |
| `/完成 <seg_id>` | `/done` | handle_urgent_response(seg_id, "completed") |
| `/延期 <seg_id> [7d/3d/1d]` | `/postpone` | handle_urgent_response(seg_id, "postponed") |

### 7.2 时间解析

```python
# src/cli/user_controls.py 新增
_TIME_ALIASES: dict[str, int] = {
    "明天": 1, "后天": 2, "下周": 7, "下月": 30,
    "尽快": 1, "马上": 1, "立刻": 1,
    "1d": 1, "3d": 3, "7d": 7, "30d": 30,
}

def parse_urgent_time(text: str, now_ms: int) -> int:
    """'明天' / '3d' / ISO 日期 / 毫秒戳 → expires_at_ms。"""
    text = text.strip().lower()
    if text in _TIME_ALIASES:
        return now_ms + _TIME_ALIASES[text] * 86400 * 1000
    if text.isdigit():
        return now_ms + int(text) * 86400 * 1000
    # ISO 日期
    try:
        from datetime import datetime
        dt = datetime.fromisoformat(text)
        return int(dt.timestamp() * 1000)
    except ValueError:
        raise ValueError(f"无法解析时间：{text!r}（支持：明天/后天/下周/3d/ISO）")
```

### 7.3 自然语言识别

```python
# 扩展 parse_natural()
_NATURAL_URGENT_RE = re.compile(
    r"(?P<sid>[0-9a-fA-F-]{8,})\s*"
    r"(?:(?:要在|截止|前完成|之前)\s*)?"
    r"(?P<when>明天|后天|下周|下月|尽快|马上|立刻|周一|周二|周三|周四|周五)"
)
```

---

## 8. GUI 4 象限视图（T34，graph_view.py 扩展）

### 8.1 视图模式

在 `gui/main_window.py` 加 tab：现有"时间线 / 树状 / 知识图谱"基础上加"**4 象限**"。

```python
# gui/quadrant_view.py（新文件）
class QuadrantView(QWidget):
    """4 象限散点图：x=importance, y=urgency。
    
    - 节点大小 = current_score
    - 节点颜色 = silence_state（active 蓝 / dormant 灰 / silent 暗灰）
    - 边框 = urgent_state（URGENT 红框 / 重要 金框）
    - 点击节点 → 主窗口打开段落详情
    - 双击 → 弹"紧急 / 重要 / 完成"对话框
    """
```

### 8.2 数据流

```python
# 复用 relations.graph.get_topic_cluster() 拉数据
# 但加一个 quadrant 维度
def list_quadrant_segments(path: Path) -> list[dict]:
    return query(
        "SELECT segment_id, topic_label, urgency_level, importance_level, "
        "current_score, silence_state, urgent_state, fog_state, "
        "expires_at_ms "
        "FROM segments "
        "WHERE current_tier != 'L3_hidden' "
        "ORDER BY current_score DESC",
        path=path,
    )
```

### 8.3 紧急提醒 widget

```python
# gui/main_window.py 加：
class UrgentAlertBar(QWidget):
    """顶部红条：当前有多少 urgent_state='expired' 段。"""
    
    def refresh(self):
        expired = query(
            "SELECT segment_id, topic_label FROM segments "
            "WHERE urgent_state = 'expired' LIMIT 5",
            path=self.db_path,
        )
        if expired:
            self.setText(f"⚠ {len(expired)} 段紧急过期待处理："
                         + " / ".join(e["topic_label"] for e in expired))
            self.show()
```

---

## 9. 任务清单 T29-T35

| T | 任务 | 工时 | 产出 | 验收 |
|---|---|---|---|---|
| **T29** | schema 扩展 + 迁移脚本 | 1d | `src/store/sqlite.py:_migrate_v2()` + 5 字段 | 旧库升级后所有新字段存在 |
| **T30** | 多因子打分公式 | 3d | `src/compress/scoring.py::calculate_score()` 替换 tick() | 4 象限各 5 段，跑 30 天模拟分数曲线合理 |
| **T31** | 情感属性提取 | 2d | `src/l0/skeleton.py::extract_emotion()` + 后台 worker | LLM 路径 + 关键词 fallback 都测；50 段测试集 emotion 准确 ≥ 80% |
| **T32** | urgent_tracker | 3d | `src/lifecycle/urgent_tracker.py` (3 函数) | 5 状态转换全覆盖；过期后 handle_response 3 路径 |
| **T33** | CLI 3 命令 | 2d | `src/cli/user_controls.py` 加 /紧急 /完成 /延期 | click + argparse 双路径；时间解析覆盖 8 种格式 |
| **T34** | GUI 4 象限视图 | 3d | `gui/quadrant_view.py` + 紧急提醒 bar | 双击节点弹对话框；Q1 区段高亮 |
| **T35** | 单测 + 验收 | 2d | `tests/test_*.py` 5 个新文件 | 全部 M2.5 任务 ≥ 90% 覆盖；旧测试不破 |
| **M2.5 总** | | **~16d / 120h** | 7 个新文件 / 1 个 schema 迁移 | 见 §11 验收清单 |

---

## 10. 测试要求

### 10.1 必须覆盖

| 类别 | 测试数目标 | 重点 |
|---|---|---|
| schema 迁移幂等性 | ≥3 个 | 旧库无字段 / 新库已有 / 重复跑都 OK |
| 多因子公式 4 象限 | ≥20 个 | Q1 不衰减 / Q4 3 天归 0 / 边界 |
| 紧急追踪状态机 | ≥15 个 | 5 状态转换 + 过期检测 + 3 响应分支 |
| 情感提取 | ≥12 个 | LLM 路径 + 关键词 fallback + 5 情感各覆盖 |
| CLI 3 命令 | ≥10 个 | 时间解析 8 种 + 自然语言 2 条 + argparse fallback |
| GUI 4 象限 | 手动测 | 截图 + 节点点击（playwright 后续补）|
| **总新增** | **≥60 个** | |

### 10.2 跑测试命令

```bash
# 跑全部（含 M1 + M2 已有）
pytest -v

# 跑 M2.5 专项
pytest tests/test_scoring_v2.py tests/test_urgent_tracker.py \
       tests/test_emotion.py tests/test_user_controls_cli.py -v

# 覆盖率（目标 ≥ 80%）
pytest --cov=src --cov-report=term --cov-report=html
```

### 10.3 验收不通过则不 commit

- 任何新代码让旧测试破 → 退回
- 覆盖率下降 → 退回
- 缺关键边界（empty/null/过期/竞争）→ 退回

---

## 11. 验收清单（M2.5 收尾）

| 项 | 指标 | 验收方式 |
|---|---|---|
| Schema 迁移 | 5 字段全部加好，旧数据不丢 | `PRAGMA table_info(segments)` + 行数比对 |
| 多因子公式 | 4 象限 half_life 正确，Q1 锁底 | 单元测试 + 30 天模拟 |
| 紧急追踪 | 5 状态全可达；expired → handle_response 3 路径 | 单元测试 + GUI 弹窗手动 |
| 情感属性 | 50 段测试集 emotion 准确 ≥ 80% | 跑测试集比对 LLM 标注 |
| CLI 命令 | /紧急 /完成 /延期 + 8 种时间格式 | click + argparse + 自然语言 |
| GUI 4 象限 | 节点拖动 + 紧急红条 + Q1 高亮 | 手动 + 截图 |
| 旧测试 | 296 个全过，零回归 | `pytest -v` |
| 第 9 铁律 | Q1 + /重要 + URGENT 段永不进 L3_hidden | 测试用例 + DB 查询断言 |
| 性能 | 1k 段 tick < 200ms | pytest-benchmark |
| 文档 | M2.5_report.md 生成 | 仿 M1_report 写 |

---

## 12. 风险与兜底

| 风险 | 等级 | 兜底 |
|---|---|---|
| 旧数据默认 (0.5, 0.5) 不准 | 🟡 中 | 跑 M2 完后做一次"AI 重打标"批量任务，LLM 给历史段补 importance / urgency |
| 情感 LLM 不可用 | 🟢 低 | 关键词 fallback 必走通（M1 不依赖 LLM）|
| 紧急追踪过期问烦用户 | 🟡 中 | 7 天内同段只问 1 次（已实现 in ask_restore）；`long_silent` 用户可强制沉默 |
| 4 象限边界 (0.7) 难调 | 🟡 中 | 留常量 `_IMPORTANCE_HIGH_THRESHOLD` + `_URGENCY_HIGH_THRESHOLD` 便于 A/B |
| GUI 4 象限性能 | 🟢 低 | 复用 graph_view.py 的 splay tree + LRU；首屏 200ms 内 |
| migration 失败数据丢失 | 🔴 高 | 迁移前自动备份 .mtca/mtca.db → .mtca/mtca.db.pre_v2 |
| 多因子公式 bug 分数乱跳 | 🟡 中 | 单元测试覆盖 4 象限各 5 段；上线前 dry-run 24h |
| urgency / importance 被用户乱调 | 🟢 低 | 这 2 列不锁（与 fog_state 同），用户改完是用户决定 |

---

## 13. 不在 M2.5 范围

- ❌ 跨设备同步（M3 再说）
- ❌ 向量召回通道（M2 已加，M2.5 复用）
- ❌ MCP Server（M2 已加）
- ❌ 自动备份（M2 末尾加，M2.5 假设已有）
- ❌ Web UI（M3）
- ❌ 矛盾检测升级（M2 已有，M2.5 不动）
- ❌ 关系图谱升级（M2 已有 T25）

---

## 14. 与现有 8 铁律的兼容性矩阵

| M2.5 改动 | 铁律 1 数据主权 | 铁律 2 L0 不可变 | 铁律 6 用户控制 | 铁律 9 永不遗忘 |
|---|---|---|---|---|
| 新增 5 字段 | ✅ 仍本地 | ✅ 不在骨架列 | ✅ 通过 update_segment 改 | ✅ Q1/URGENT 锁底 |
| 多因子公式 | ✅ | ✅ 旧触发器不动 | ✅ 用户可调 urgency | ✅ 满足 |
| urgent_tracker | ✅ | ✅ | ✅ /紧急 /完成 /延期 | ✅ URGENT 状态不衰减 |
| 情感属性 | ✅ | ✅ | ✅ 用户可覆盖 | — 中性 |
| CLI 3 命令 | ✅ | ✅ | ✅ 新增即合规 | — |
| GUI 4 象限 | ✅ | ✅ | ✅ 视图层 | ✅ Q1 高亮 |

**全绿，无违反**。

---

## 15. 下一步行动

老板你选：

1. **直接动手** —— 我开 T29 schema 迁移脚本（一个 `.py` 文件，幂等可重跑），跑通后给 T29-T35 详细 prompt
2. **先细化 T32** —— 把紧急追踪流程画 1 张时序图 + 状态机表，再写代码
3. **先 P0 收尾** —— M1 报告的 3 个 P0 致命问题（mtca-server 密钥 / pyproject 入口 / setup.py 空）必须先修，再做 M2.5
4. **保留** —— 这版先存档，等 M2 跑完再启动 M2.5

我推荐 **3 → 1**：先把 M1 验收 3 个 P0 修掉（不修别上 M2），然后开 T29。

---

**文档结束**。下一步等你点头。