# MTCA M1 代码审查报告（v0.4）

> **审查时间**：2026-07-05
> **审查范围**：`src/` + `gui/` + `tests/` + `docs/` + 配置
> **测试基线**：296 测试 / 100% 通过 / 行覆盖率 73%
> **审查模式**：只读（不修改源码）

---

## 第 1 阶段：代码全量扫描

| 项 | 结果 | 评估 |
|---|---|---|
| 文件总数 | 60 个 `.py`（src 24 + tests 15 + gui 7 + 其他 14） | — |
| `import SQLAlchemy / peewee` | 0 个 | ✅ 符合 §6.3 |
| `pylint / flake8` 配置 | 0 个 | ✅ |
| `TODO / FIXME / HACK / XXX` | 0 个真实标记（仅 test 字面量 `"xxx"`） | ✅ 干净 |
| `print()` 调用 | 13 处（10 处在 CLI `main()` 出口 + 3 处 `console.print` rich 渲染 + 1 处 smoke） | ⚠️ 可改 logger |
| f-string 内嵌 SQL（注入风险） | 0 个数据点插入；仅 `?` 占位符或硬编码标识符 | ✅ 无注入 |
| bare `except:` | 0 个 | ✅ |
| `except Exception` | 16 处 | ⚠️ 部分应窄化 |
| 函数 > 80 行 | 12 个（38 个 > 50 行项目上限） | ⚠️ 违反 §13.2 |
| 文件 > 任务行数限制 | 0 个（最大 `src/llm/provider.py` 699 行正好踩 700 上限） | ⚠️ 临界 |

**f-string SQL 安全复核**（`SELECT-INSERT-UPDATE-FROM-WHERE` 模式共 16 处）：逐条检查后**全部安全**——均使用 `IN ({placeholders})` / `LIKE ?` / 列名白名单 / 硬编码表名。

---

## 第 2 阶段：架构审查

### 2.1 8 条铁律代码落地

| # | 铁律 | 代码位置 | 评估 |
|---|------|----------|------|
| 1 | 数据主权在用户 | `MTCA_DB_PATH = Path.home() / ".mtca"` (`src/store/sqlite.py:46`) | ✅ |
| 2 | L0 双层 + AI 不可变 | 触发器 `no_update_messages_ai` / `no_delete_messages` / `no_update_skeleton` | ✅ |
| 3 | L1–L3 是视图 | `views` 表 `tier` + `expires_at` + `is_stale` | ✅ |
| 4 | 雾化 = 用户主动丢弃细节 | `fog_engine.fog_segment` + `fog_session` 控制表 | ✅ |
| 5 | 召回错误自动回 L0-骨架 | `recall_with_fallback` → `get_recent_sessions` | ✅ |
| 6 | 用户控制接口直达 L0 | `src/cli/user_controls.py` 4 命令 | ⚠️ **0% 覆盖** |
| 7 | GUI 优先 | `gui/main_window.py` + `timeline_view.py` + `fog_dialog.py` + `graph_view.py` | ✅ |
| 8 | 存储成本由用户承担 | 单文件 SQLite + WAL | ✅ |

### 2.2 6 张主表 + 1 张控制表

| 表 | 设计文档 | 代码实现 | 一致 |
|---|---|---|---|
| `sessions` | DEVELOPER_PLAN §2.2 表 1 | `_SCHEMA_SQL` 第 58-74 行 | ✅ |
| `messages` + `messages_fts` | 表 2 | 第 77-117 行 | ✅ |
| `segments` | 表 3 | 第 121-155 行 | ✅ |
| `segment_relations` | 表 3.5 | 第 158-176 行 | ✅ |
| `views` | 表 4 | 第 179-196 行 | ✅ |
| `score_events` | 表 5 | 第 199-211 行 | ✅ |
| `fog_session`（控制表） | DATA_MODEL §2.3 | 第 215-219 行 | ✅ |

### 2.3 触发器（6 个）

- `no_update_messages_ai` / `no_delete_messages` / `no_update_skeleton`（L0 保护）
- `messages_fts_ai` / `messages_fts_ad` / `messages_fts_au`（FTS5 同步）

**潜在点**：`no_update_skeleton` 是 `BEFORE UPDATE OF <col>`，仅当 SET 子句显式提到列才触发。项目内所有路径走 `update_segment` + `_UPDATABLE_FIELDS` 白名单，**实际安全**。

### 2.4 模块依赖图

```
src/store/sqlite            ← 基础层
        ↑
src/l0/{session_writer, skeleton, time_segmenter, segment_writer}
        ↑
src/{fog, recall, compress/scoring}  +  src/llm
        ↑
src/lifecycle/*  +  src/relations  +  src/compress/regen_engine
        ↑
src/cli/*  +  gui/*
```

无环，依赖方向单调，**符合 PLAYBOOK 设计**。

---

## 第 3 阶段：测试审查

### 3.1 测试运行结果

```
296 passed in 31.45s
TOTAL                          2703    738    73%
```

### 3.2 模块覆盖率（升序）

| 模块 | 覆盖率 | 行数 | 备注 |
|---|---|---|---|
| `src/cli/timeline.py` | **0%** | 229 | ⚠️ 全部 229 行未被测 |
| `src/cli/user_controls.py` | **0%** | 158 | ⚠️ 4 个核心命令全无测 |
| `src/llm/provider.py` | 65% | 357 | 4 后端未全覆盖；cloud 假数据测得多 |
| `src/lifecycle/contradiction_detector.py` | 77% | 236 | M2 异步路径未测 |
| `src/store/sqlite.py` | 79% | 92 | `fog_revoke` 死分支未测 |
| `src/lifecycle/retention_engine.py` | 84% | 174 | `force_silent` 略低 |
| `src/fog/fog_engine.py` | 87% | 71 | 核心路径全测 |
| `src/compress/scoring.py` | 87% | 141 | `has_been_referenced` 边界 |
| `src/recall/recall_engine.py` | 88% | 230 | FTS5 特殊字符单测覆盖好 |
| `src/l0/session_writer.py` | 88% | 59 | 主流程测 |
| `src/l0/time_segmenter.py` | 91% | 126 | 3 规则 + 弱合并测到 |
| `src/compress/regen_engine.py` | 92% | 195 | 4 触发器 + 重试测到 |
| `src/relations/graph.py` | 93% | 286 | BFS / 4 关系 / 导出测到 |
| `src/l0/segment_writer.py` | 93% | 69 | 字段白名单测到 |
| `src/lifecycle/ask_restore.py` | 96% | 101 | 5 响应分支测到 |
| `src/recall/fog_protocol.py` | 98% | 89 | 33 个测试覆盖 |
| `src/l0/skeleton.py` | 98% | 90 | jieba + 中文 + 边界 |

### 3.3 铁律测试覆盖

| 铁律 | 有测试 | 测试位置 |
|---|---|---|
| 1 数据主权 | 隐式 | `test_pragmas_applied` |
| 2 L0 不可变 | ✅ 3 个 | `test_no_update_messages_blocks_ai` / `test_no_delete_messages` / `test_fog_permit_*` |
| 3 L1–L3 视图 | ⚠️ 文档无单测 | views 表存在，无 `test_views_*` |
| 4 雾化 | ✅ 8 + 33 个 | `tests/test_fog_engine.py` + `tests/test_fog_protocol.py` |
| 5 L0 兜底 | ✅ 1 个 | `test_recall_falls_back_to_recent` |
| 6 用户控制 | ❌ **0 个** | 4 个 CLI 命令全无测 |
| 7 GUI | ❌ 无自动 | 仅手动测（playwright 未跑） |
| 8 存储成本 | N/A | — |

---

## 第 4 阶段：文档审查

### 4.1 文档过期项

| 位置 | 现状 | 实际代码 | 状态 |
|---|---|---|---|
| `README.md` "M1 ≈ 50h" | 写 50h | M1 报告承认 ~95h | ⚠️ 过期 |
| `README.md` "技术栈待定（PySide6 / Tauri / Electron）" | 写 TBD | 已确定 PySide6 | ⚠️ 过期 |
| `pyproject.toml:30` | `[project.scripts]` 写 `mtca-server = "src.cli.server:main"` | `src/cli/server.py` **不存在** | 🔴 失效 |
| `setup.py` | 0 字节空文件 | 应有 `setup()` 或删除 | 🔴 失效 |
| `benchmarks/M1_report.md §4` | 写 64% 覆盖率 | 实际 73% | ⚠️ 过期 |

### 4.2 与 M1 验收报告的差异

- 报告自报 64% 覆盖 → 实测 73%（M2 测试加进来）
- 报告说 `setup.py` 存在 → 实际是空文件
- 召回准确率"未量化" → 50 段 + 20 边界共 70 测试，**但仅断言"至少返回 1 个"**，未断言 top-1 命中

---

# 整改方案（按优先级）

## A. 致命问题（必须修）

### A1. 🔴 硬编码第三方服务凭证(凭据已清理)

> **历史问题**:早期 commit 中存在硬编码的 tracer 凭据,**已于 2026-08-10 在 branch `cleanup/remove-leaked-credentials-and-pii` 中清理**:
> - API key 已在第三方平台 revoke
> - 配置文件改为读环境变量(见下方整改方案)
> - 当前 branch 已不含明文凭据
> - 注:`mtca-server.py` 若仍存在于历史 commit 中,需用 BFG / filter-repo 清理历史(详见 PR 说明)

**【整改方案】**(已应用)
`python
import os

if os.environ.get("MTCA_TRACE") == "1":
    from syncause_tracer import initialize
    initialize(
        api_key=os.environ["SYNCAUSE_API_KEY"],
        proxy=os.environ["SYNCAUSE_PROXY"],
        app_name="MTCA",
        project_id=os.environ["SYNCAUSE_PROJECT_ID"],
    )
`
**【优先级】** P0(已修复)### A2. 🔴 `pyproject.toml` 引用不存在的 console_script

**【位置】** `I:\PROJECTS\MTCA\pyproject.toml:30`

**【问题】**
```toml
[project.scripts]
mtca-server = "src.cli.server:main"   # ← src.cli.server 模块不存在
mtca = "src.cli.timeline:main"
```
- `src/cli/server.py` 实际**不存在**
- `setuptools` 打包会**直接报错**，`pip install -e .` 失败
- M1 验收"安装门槛"指标直接不达标

**【影响】** 高：M1 验收不达标

**【整改方案】**
**方案 a**（推荐）：把 `mtca-server.py` 移到 `src/cli/server.py` 并改写为模块，`pyproject.toml` 不用改：
```python
# src/cli/server.py
import click
from src.store.sqlite import init_db, get_stats, MTCA_DB_PATH

@click.group()
def main():
    """MTCA - Multi-Tier Context Architecture"""
    pass

@main.command()
def init():
    init_db()
    click.echo(f"✓ DB @ {MTCA_DB_PATH}")
# ... 等价的 3 个子命令
```
**方案 b**：修改 `pyproject.toml` 指向 `mtca-server.py` 根目录入口（不推荐）。

**【优先级】** P0

---

### A3. 🔴 `setup.py` 是 0 字节空文件

**【位置】** `I:\PROJECTS\MTCA\setup.py`（0 行）

**【问题】** `setup.py` 与 `pyproject.toml` 并存时，空文件可能干扰老工具链（`setuptools<61`）

**【影响】** 中：现代工具无影响

**【整改方案】**
```bash
# (a) 推荐：删除（纯 PEP 621 风格）
git rm setup.py
# (b) 或留空但加注释说明
echo "# Legacy file. Build config lives in pyproject.toml (PEP 621)." > setup.py
```

**【优先级】** P0

---

## B. 重要问题（应该修）

### B1. ⚠️ `src/cli/*` 覆盖率 0%——铁律 6 核心命令无自动化保护

**【位置】** `src/cli/timeline.py`（229 行）+ `src/cli/user_controls.py`（158 行）

**【问题】** 4 个核心命令（`/重要 /循环 /归档 /雾化`）+ `parse_command` / `parse_natural` 中文解析器**完全无 pytest 覆盖**。

**【影响】** 中：用户高频直接调用的接口无人守护

**【整改方案】**
新增 `tests/test_user_controls_cli.py`（≥ 30 个测试）：
```python
def test_cmd_important_invokes_mark_important()
def test_cmd_cycle_writes_tag()
def test_cmd_archive_sets_tier_l3_hidden()
def test_cmd_fog_calls_fog_engine_with_user_caller()

def test_parse_command_slash_important()
def test_parse_command_slash_cycle_with_tag()
def test_parse_command_in_middle_of_text()
def test_parse_command_invalid_returns_none()

def test_parse_natural_chinese_keyword()
def test_parse_natural_fog_with_anchor()
def test_parse_natural_cycle_with_tag()

def test_argparse_fog_requires_anchor()
def test_argparse_invalid_command_exits_2()
```
预期覆盖率：0% → 80%+

**【优先级】** P1

---

### B2. ⚠️ 38 个函数超过项目自身 50 行限制

**【位置】** 12 个 > 80 行的函数：

| 行数 | 文件:函数 |
|---|---|
| 135 | `src/fog/fog_engine.py:168 fog_segment` |
| 106 | `gui/graph_view.py:468 _build_ui` |
| 100 | `gui/graph_view.py:791 _force_layout` |
| 92 | `src/compress/regen_engine.py:247 regen_view` |
| 92 | `gui/graph_view.py:675 _apply_filters` |
| 90 | `src/l0/session_writer.py:156 write_message` |
| 88 | `src/lifecycle/contradiction_detector.py:406 user_override` |
| 87 | `src/recall/recall_engine.py:485 recall` |
| 86 | `src/compress/scoring.py:263 tick` |
| 83 | `gui/segment_detail.py:46 __init__` |
| 82 | `src/relations/graph.py:520 auto_create_relations` |
| 80 | `gui/fog_dialog.py:50 __init__` |

**【整改方案】**（以 `fog_segment` 为例）拆 6 步事务为 5 个小函数：
```python
def _open_fog_channel(conn, session_id, now_ms) -> None
def _erase_messages_in_segment(conn, session_id, start_seq, end_seq) -> int
def _mark_segment_fogged(conn, segment_id, now_ms) -> None
def _write_audit_event(conn, segment_id, anchor, now_ms) -> None
def _close_fog_channel(conn, session_id) -> None

def fog_segment(...):
    # 主入口只剩 30 行：参数校验 + 幂等检查 + 调上面 5 个 + finally
```

**【优先级】** P1

---

### B3. ⚠️ 16 处 `except Exception` 范围太宽

**【位置】** `src/fog/fog_engine.py:291` / `src/lifecycle/retention_engine.py:462,503` / `src/lifecycle/contradiction_detector.py:186,513,518` / `src/llm/provider.py:501,516,528,787,832` 等

**【问题】** `except Exception as exc: pass` 把 `KeyboardInterrupt` 之外几乎所有错误吞掉，调试困难。

**【整改方案】**
- `src/fog/fog_engine.py:291` 雾化异常兜底 → 捕获 `RuntimeError` + `sqlite3.Error`
- `src/lifecycle/retention_engine.py:462,503` 后台循环 → 捕获 `RuntimeError` + `logger.warning`（不是 `pass`）
- `src/llm/provider.py:501` 已有 `# llama-cpp 抛异常类型不可控` 注释 → 保留但加 log
- 其他 11 处逐条检查并窄化

**【优先级】** P1

---

### B4. ⚠️ 召回准确率未量化

**【位置】** `src/recall/recall_engine.py:605` + `benchmarks/M1_report.md §附录A`

**【问题】** M1 报告自承"召回准确率 ≥ 90% 指标未量化报告"；目前 50 段测试**断言"至少返回 1 个"**，**没有**断言"top-1 是正确答案"。

**【整改方案】**
新增 `tests/test_recall_accuracy.py`：
```python
@pytest.mark.parametrize("case", load_test_set())
def test_recall_top1_hit_rate(case):
    results = recall(case.query, top_k=1, path=mtca_db)
    if results:
        assert results[0]["segment_id"] == case.expected_segment_id
```
加 `benchmarks/accuracy_report.py` 跑 hit_rate@1 / hit_rate@3 / MRR，存为 markdown。

**【优先级】** P1

---

### B5. ⚠️ README.md 与现实不符

**【位置】** `I:\PROJECTS\MTCA\README.md:13,86-93`

**【问题】**
- 路线图写 "M1 ≈ 50h"——实际 ~95h
- "技术栈待定"——已确定 PySide6

**【影响】** 中：给新读者错误印象

**【整改方案】** README 属 AI_RULES §15 锁定文件，**本报告仅列出问题**，由用户决定是否解锁修改。

**【优先级】** P1

---

## C. 改进建议（建议修）

### C1. CLI `print()` 改 logger
- **【位置】** `src/cli/timeline.py:375,378,380` + `src/cli/user_controls.py:326-337`
- **【方案】** 引入 `logging.getLogger("mtca.cli")`，5+2 个 print 改 `logger.info` / `logger.error`，方便接 logfile。

### C2. `recall_engine._sanitize_fts_query` 正则可一步
- **【位置】** `src/recall/recall_engine.py:106`
- **【方案】** 合并两次 `re.sub` 为一行。

### C3. `_build_topics_filter` 与 `_build_time_filter` 重复
- **【位置】** `src/recall/recall_engine.py:148-194`
- **【方案】** 抽 `_build_in_filter(column, values)`。

### C4. `parse_verdict` 的 alias 字典提升为模块常量
- **【位置】** `src/lifecycle/contradiction_detector.py:217-223`
- **【方案】** `_LEVEL_ALIAS` 提升为模块级常量。

### C5. `regen_engine` 同步入口 `time.sleep` 可能阻塞
- **【位置】** `src/compress/regen_engine.py:282-295`
- **【方案】** `process_queue` 增加 `synchronous=False` 选项。

---

## D. 信息项（仅记录）

### D1. M1 验收基线偏差

| 项 | M1 报告 | 实际 |
|---|---|---|
| 整体覆盖率 | 64% | 73% |
| 主召回 p95 | 145ms @ 1k 段 | 同（已承认超 50ms 目标） |
| 安装门槛 | 未跑 | **未跑**（A2 修复后会立刻失败） |
| 召回准确率 ≥ 90% | 未量化 | 仍 ⚠️ 未量化 |
| 时间分段人工抽检 | 未抽检 | 仍 ⚠️ 未抽检 |
| 24h 模拟 L1 ≥ 95% | 未跑 | 仍 ⚠️ 未跑 |

### D2. M2 待办（T20-T27）实际状态

| T | 状态 |
|---|---|
| T20 retention 引擎 | ✅ 已实现 + 17 测试 |
| T21 AI 询问升级 | ✅ 已实现 + 12 测试 |
| T22 矛盾检测 | ✅ 已实现 + 24 测试 |
| T23 关系图谱 | ✅ 已实现 + 43 测试 |
| T24 摘要重写 | ✅ 已实现 + 30 测试 |
| T25 GUI 知识图谱 | ✅ 已实现（`graph_view.py` 821 行） |
| T26 关系图测试 | ✅ = T23 测试 |
| T27 重写测试 | ✅ = T24 测试 |
| T28 竞品分析 | ✅ 已完成 |

> 注：T20-T25 已完成实现 + 测试 + GUI，但 M1 报告划入 M2。**8 个 T 任务**未在 M1 验收打勾。

### D3. 已记录的边界
- FTS5 `unicode61` 对 CJK 简单切分（不做新词发现）—— `test_edge_fts5_special_chars_query` 已覆盖
- `regen_engine` 后台 worker 是 daemon 线程
- `mtca-server.py` 的 `start` 是 stub（M1 阶段无真服务）
- 4 个 GUI 页面手动测过，**playwright 自动截图未跑**

### D4. TODO / FIXME 实际标记
**0 个真实标记**。`grep TODO/FIXME/HACK` 仅在 test 字面量 `"xxx"` / `"no-such-segment-uuid-xxx"` / `"XXXXX"` 中匹配，**非真实待办**。

### D5. 数据集与文档完整性
- `benchmarks/test_set.json` 50 段（30 真历史 + 20 边界）✅
- 9 份设计文档（README + DEVELOPER_PLAN + 7 份 docs/*.md）✅
- `mtca-server.py` 占位 + `pyproject.toml` 声明 ✅（但 A2 失效）
- 中文错误信息 + 中文 docstring ✅

---

# 摘要

| 分类 | 数量 | 关键 |
|---|---|---|
| 🔴 致命 P0 | 3 | 硬编码密钥 / 入口失效 / setup.py 空 |
| 🟡 重要 P1 | 5 | CLI 0% 覆盖 / 38 函数超限 / 宽 except / 准确率未量化 / README 过期 |
| 🟢 改进 P2 | 5 | print 改 logger / 正则精简 / 重复代码 / 常量提升 / 同步阻塞 |
| ⚪ 信息 D | 5 | M1 基线偏差 / T20-T25 已实现 / 0 TODO / 数据集齐 / 文档齐 |

**总测试**：296 通过 / 73% 覆盖 / 100% 通过率
**M1 主流程**：闭环 ✅（21/28 T 任务完成 + 报告自承的 4 项 M2 待办已悄悄做完）
**M1 验收基线**：3 处阻塞（致命 P0 全部修掉后即可重新跑 `pip install -e .` + pytest + `mtca-server` 启动验收）

---

**审查完成时间**：2026-07-05
**下次复审建议**：A1-A3 修复后 + B1（CLI 覆盖）补齐后